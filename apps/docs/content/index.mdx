import { Tabs } from 'nextra/components';

# lamstack

A collection of independent, headless React packages. MIT licensed. Each package installs
on its own and only depends on `@lamstack/react-core` — never on a sibling feature package.

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tabs.Tab>
    ```bash
    pnpm add @lamstack/react-dialog
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    npm install @lamstack/react-dialog
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    yarn add @lamstack/react-dialog
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    bun add @lamstack/react-dialog
    ```
  </Tabs.Tab>
</Tabs>

## Packages

- [`@lamstack/react-dialog`](/dialog) — headless, imperative dialog API
  (`useDialogs()` → `open`/`close`/`alert`/`confirm`/`prompt`).
- [`@lamstack/react-initializer`](/initializer) — headless app-startup orchestrator
  (`<Initializer>` runs a dependency graph of async tasks, with retry, timeout, and
  critical/non-critical failure handling).

More packages (data, ...) are planned.
